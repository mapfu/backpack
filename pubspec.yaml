name: backpack
description: Dart/Flutter tools.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+2
environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  logger: 1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^1.0.4

flutter:

  uses-material-design: true

